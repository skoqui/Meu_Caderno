**O que é?**  
Funções anônimas e curtas, usadas quando a função tem apenas uma linha.

```python
lambda args: expressão
```

**Exemplos:**

Ordenar por tamanho:
```python
sorted(lista, key=lambda x: len(x))
```


